{
  "name": "micro-frontend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "host",
    "remote"
  ],
  "scripts": {
    "start": "concurrently \"yarn workspace host start\" \"yarn workspace remote start\"",
    "build": "yarn workspace host build && yarn workspace remote build && mkdir -p public && cp -r host/dist/* public/"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
